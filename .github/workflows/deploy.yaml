name: Provision and Deploy

on:
  push:
    branches:
      - feature/my-change-updated
    tags:
      - 'deploy-*'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: ap-south-1
      PEM_KEY: ${{ secrets.PEM_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Terraform Init
        working-directory: terraform
        run: terraform init

      - name: Cleanup Terraform state for deleted resources
        working-directory: terraform
        run: |
          terraform state rm aws_iam_role.s3_writeonly_role || true
          terraform state rm aws_iam_policy.s3_writeonly_policy || true
          terraform state rm aws_iam_role_policy_attachment.s3_writeonly_attach || true
          terraform state rm aws_iam_role.s3_readonly_role || true
          terraform state rm aws_iam_policy.s3_readonly_policy || true
          terraform state rm aws_iam_role_policy_attachment.s3_readonly_attach || true
          terraform state rm aws_iam_instance_profile.ec2_instance_profile || true
          terraform state rm aws_s3_bucket.log_bucket || true

      - name: Terraform Apply
        working-directory: terraform
        run: terraform apply -auto-approve

      - name: Save PEM file
        run: |
          echo "${PEM_KEY}" > mykey.pem
          chmod 600 mykey.pem

      - name: Run Deploy Script
        working-directory: script
        run: bash script/deploy2.sh
